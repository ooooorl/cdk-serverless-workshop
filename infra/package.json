{
  "name": "infra",
  "version": "0.1.0",
  "bin": {
    "infra": "bin/infra.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "test": "jest",
    "cdk": "cdk",
    "diff:staging": "npm run cdk -- diff staging-GlobalStack --require-approval never",
    "diff:prod": "npm run cdk -- diff prod-GlobalStack --require-approval never",
    "deploy:staging": "npm run cdk -- deploy staging-GlobalStack --require-approval never",
    "deploy:prod": "npm run cdk -- deploy prod-GlobalStack --require-approval never"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "@types/node": "22.7.9",
    "aws-cdk": "2.1029.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "typescript": "~5.6.3"
  },
  "dependencies": {
    "aws-cdk-github-oidc": "^2.4.1",
    "aws-cdk-lib": "2.213.0",
    "constructs": "^10.0.0"
  }
}
